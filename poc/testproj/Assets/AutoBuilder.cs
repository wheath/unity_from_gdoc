// Simple script that lets you create a new
// scene, create a cube and an empty game object in the scene
// Save the scene and close the editor
	
using UnityEditor;
using UnityEngine;
using System.IO;

public static class AutoBuilder {
  static void testFunc() {
    Debug.Log("Hello");  
    //FileStream fileStream = new FileStream(@"file.txt", FileMode.Open, FileAccess.Write);
    //fileStream.Close();

  }
}	
